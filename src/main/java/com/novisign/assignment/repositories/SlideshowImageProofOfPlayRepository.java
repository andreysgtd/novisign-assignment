package com.novisign.assignment.repositories;

import com.novisign.assignment.models.SlideshowImageProofOfPlay;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SlideshowImageProofOfPlayRepository extends JpaRepository<SlideshowImageProofOfPlay, Long> {
}
